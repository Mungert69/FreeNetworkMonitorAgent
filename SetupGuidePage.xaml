<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="NetworkMonitorAgent.SetupGuidePage"
             Title="Setup Guide">

    <ScrollView>
        <VerticalStackLayout Spacing="15" Padding="10" VerticalOptions="Center">

            <!-- Title Section with Logo -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}" Padding="20" CornerRadius="10" HorizontalOptions="FillAndExpand">
                <VerticalStackLayout Spacing="10">
                    <Image Source="logo.png" SemanticProperties.Description="Free Network Monitor Logo" HeightRequest="100" HorizontalOptions="Center" />
                    <Label Text="Free Network Monitor Agent Setup Guide" FontSize="22" HorizontalOptions="Center" TextColor="{DynamicResource Primary}" />
                </VerticalStackLayout>
            </Frame>

            <!-- Initial Configuration Section -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}" Padding="15" CornerRadius="10" HorizontalOptions="FillAndExpand">
                <VerticalStackLayout>
                    <Label  Padding="15"  HorizontalOptions="Center" Text="Welcome to the Free Network Monitor Agent! This guide will help you set up and configure the app to monitor your network." FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                  
                </VerticalStackLayout>
            </Frame>

            <!-- Setup Steps Section -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}" Padding="15" CornerRadius="10" HorizontalOptions="FillAndExpand">
                <VerticalStackLayout x:Name="SetupStepsContainer">
                    <!-- Default content (can be hidden/shown as needed) -->
                    <VerticalStackLayout x:Name="DefaultSteps">
                        <Label Padding="15" HorizontalOptions="Center" Text="Setup Monitoring" FontSize="18" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />

                        <Label HorizontalOptions="Center" Padding="5" Text="Step 1: Enable the Agent" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                        <Label Text="To begin, toggle the 'Enable Agent' slider to the 'on' position on the main screen. This activates the agent and displays additional setup options."/>

                        <Label HorizontalOptions="Center" Padding="5" Text="Step 2: Authorize Your Device" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                        <Label Text="To operate as a network monitoring agent, your device needs authorization. Tap 'Authorize,' which will redirect you to an OAuth authentication page. Log in or create an account if needed. Once you see a confirmation message, close the browser window."/>

                        <Label HorizontalOptions="Center" Padding="5" Text="Step 3: Access the Free Network Monitor Dashboard" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                        <Label Text="After authorization, tap 'Login to Free Network Monitor' to access the dashboard at https://freenetworkmonitor.click/dashboard using the same email you used for authorization. This is where you manage and monitor network activities, and it's also where the website version of the Free Network Monitor Assistant is available to help with advanced tasks and insights." />

                        <Label HorizontalOptions="Center" Padding="5" Text="Step 4: Add Hosts to Monitor" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                        <!-- Rest of your default content -->
                    </VerticalStackLayout>

                    <!-- Alternate content (initially hidden) -->
                    <VerticalStackLayout x:Name="AlternateSteps" IsVisible="False">
                        <Label Padding="15" HorizontalOptions="Center" Text="Setup Monitoring" FontSize="18" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />

                        <Label HorizontalOptions="Center" Padding="5" Text="Step 1: Enable the Agent" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                        <Label Text="To begin, toggle the 'Enable Agent' slider to the 'on' position on the main screen. This activates the agent and displays additional setup options."/>

                        <Label HorizontalOptions="Center" Padding="5" Text="Step 2: Authorize Your Device" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                        <Label Text="To operate as a network monitoring agent, your device needs authorization. Tap 'Authorize,' which will redirect you to an OAuth authentication page. Log in or create an account if needed. Once you see a confirmation message, close the browser window."/>

                        <Label HorizontalOptions="Center" Padding="5" Text="Step 3: Open the Free Network Monitor Assistant" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                        <Label Text="After authorization, you can directly access the Free Network Monitor Assistant for all your monitoring needs and advanced tasks." />
                    </VerticalStackLayout>
                </VerticalStackLayout>
            </Frame>
            <!-- Viewing Monitoring Data Section -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}" Padding="15" CornerRadius="10" HorizontalOptions="FillAndExpand">
                <VerticalStackLayout>
                    <Label  Padding="15"  HorizontalOptions="Center" Text="Viewing Monitoring Data" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                    <Label Text="You can view detailed monitoring data in three ways: through the Free Network Monitor Dashboard, within the app's Host Data tab, or by using the Free Network Monitor Assistant."/>
                    <Label Text="• Using the Free Network Monitor Dashboard: Access the dashboard to view comprehensive monitoring data and analyses for each host."/>
                    <Label Text="• Using the Host Data Tab in the App: Host data is visually represented in the app with indicator circles for each host. Each circle will appear green if the host is active or red if down."/>
                    <Label Text="  - Tap the Circle: Tap a circle to view detailed monitoring information for the corresponding host."/>
                    <Label Text="  - Pulsing Circles: A pulsing circle signifies that the host is up, and the pulse rate reflects the response time: the faster the pulse, the faster the response time"/>
                    <Label Text="  - Purple Beacon Effect: This effect represents the success rate of host responses. A smaller beacon indicates a lower success rate, providing a visual indicator of reliability for each host."/>
                    <Label Text="• Using the Free Network Monitor Assistant: You can ask the assistant questions about your monitoring data. The website version of the assistant not only displays the requested data but also automatically switches views in the dashboard to show the relevant information, providing an interactive way to access insights and updates quickly."/>
                </VerticalStackLayout>
            </Frame>

            <!-- Alerts and Reports Section -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}" Padding="15" CornerRadius="10" HorizontalOptions="FillAndExpand">
                <VerticalStackLayout>
                    <Label  Padding="15"  HorizontalOptions="Center" Text="Setting Up Alerts and Reports" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                    <Label Text="To receive alerts when a host is down, verify your email. Weekly performance reports are also available. If you don't receive a verification email, ensure support@mahadeva.co.uk is whitelisted in your spam filte."/>
                </VerticalStackLayout>
            </Frame>

            <!-- Advanced Features Section -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}" Padding="15" CornerRadius="10" HorizontalOptions="FillAndExpand">
                <VerticalStackLayout>
                    <Label Padding="15"   HorizontalOptions="Center" Text="Using Advanced Features" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                    <Label Text="The Free Network Monitor Assistant provides advanced capabilities, including Nmap scans, OpenSSL checks, and BusyBox commands. You can also perform internet searches for technical assistance."/>
                    <Label Text="Note for Windows Users: The assistant can also utilize Metasploit for penetration testing, but you must install the Metasploit Framework separately. After installation, ensure 'msfconsole' is accessible from the system's PATH."/>
                    <Label Text="Note for Android Users: The Metasploit framework is not available for Android at the time of publishing."/>
                    <Label Text="Important: Ensure you have permission to run any security tests, scans, or commands on systems being tested. Unauthorized testing can lead to legal consequences."/>
                </VerticalStackLayout>
            </Frame>

            <!-- Using the Free Network Monitor Assistant with Your Agent -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}" Padding="15" CornerRadius="10" HorizontalOptions="FillAndExpand">
                <VerticalStackLayout>
                    <Label  Padding="15"  HorizontalOptions="Center" Text="Using the Free Network Monitor Assistant with Your Agent" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                    <Label Text="The website version of the Free Network Monitor Assistant is available at https://freenetworkmonitor.click/dashboard ; click the assistant icon bottom right on the page. The in app Assistant is available in the Chat enabled version of The Free Network Monitor Agent. The Free Network Monitor Assistnat enables advanced security tests, penetration assessments, BusyBox commands, and internet search functions. Acting as an intermediary, the assistant supports these capabilities:"/>
                    <Label Text="• Nmap Scans: Initiate network scans to discover open ports, detect devices, and assess vulnerabilities. Simply request a scan on a specific host, and the assistant will display results, including open ports and running services."/>
                    <Label Text="• Metasploit Modules: Perform targeted penetration testing on Windows by running Metasploit modules. Request a module to simulate real-world attacks and receive a detailed vulnerability report."/>
                    <Label Text="• OpenSSL Checks: Audit SSL/TLS configurations on your network to ensure encryption settings are secure and up to date, helping protect data in transit."/>
                    <Label Text="• BusyBox Commands: Execute BusyBox commands for tasks like file management, networking, and process control. Commands such as ls, ifconfig, and ping are supported, streamlining device management and troubleshooting."/>
                    <Label Text="• Internet Search Capabilities: Find answers to technical questions, solutions to errors, or additional documentation. Ask the assistant to search for specific information, and it will return relevant articles or results directly on your dashboard."/>
                    <Label Text="Using the assistant’s capabilities, you can perform these tasks without needing to manually configure or run tools. The assistant will execute commands, display results, search the web, and help interpret findings, making complex tasks straightforward and efficient."/>
                </VerticalStackLayout>
            </Frame>

            <!-- Download Section -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}" Padding="15" CornerRadius="10" HorizontalOptions="FillAndExpand">
                <VerticalStackLayout>
                    <Label  HorizontalOptions="Center" Text="Download the Free Network Monitor Agent on Various Platforms:" FontSize="16" FontAttributes="Bold" Padding="15" TextColor="{DynamicResource Primary}" />

                    <Label Text="The Free Network Monitor Agent is available for multiple platforms, each offering unique benefits:"/>
                    <Label Text="• Android Version: Ideal for portable network monitoring, accessible directly on your mobile device. Note: Android’s battery-saving features may affect polling frequency when the device is not charging. The Metasploit framework is not available for Android."/>
                    <Label Text="• Windows Version: Provides full functionality, including support for advanced security tools like Metasploit. Install the Metasploit framework separately to enable penetration testing features. This version is well-suited for in-depth network assessments."/>
                    <Label Text="• Docker Version: Offers flexible deployment on servers or containers, ideal for centralized monitoring across network environments. The Docker version supports all major monitoring tools and functions, making it highly scalable and customizable."/>
                    <Label Text=" "/>
                    <!-- Refined Button -->
                    <!-- Slimmer, Link-like Button -->
                     <Button x:Name="DownloadLink"
                            Text="Visit Download Page"
                            FontSize="12"
                            Clicked="OnDownloadLinkClicked"
                            HorizontalOptions="Center"
                            CornerRadius="8"/>

                </VerticalStackLayout>
            </Frame>



            <!-- Support Section -->
            <Frame BackgroundColor="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource Gray950}}" Padding="15" CornerRadius="10" HorizontalOptions="FillAndExpand">
                <VerticalStackLayout>
                    <Label Padding="15"  HorizontalOptions="Center"  Text="Support" FontSize="16" FontAttributes="Bold" TextColor="{DynamicResource Primary}" />
                    <Label  HorizontalOptions="Center"  Text="For assistance, feedback, or queries, contact support at support@mahadeva.co.uk. We're here to help!"/>
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>

